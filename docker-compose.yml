services:
  app:
    build: .
    image: finally
    container_name: finally-app
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  finally-data:
    name: finally-data
