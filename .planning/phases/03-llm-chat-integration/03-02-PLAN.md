---
phase: 03-llm-chat-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/app/main.py
  - backend/tests/api/test_chat.py
autonomous: true
requirements:
  - API-07
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06

must_haves:
  truths:
    - "POST /api/chat is reachable at http://localhost:8000/api/chat"
    - "With LLM_MOCK=true the test suite confirms mock response shape and trade execution"
    - "Test suite confirms fallback response when LLM raises an exception"
    - "Test suite confirms user and assistant messages are persisted in chat_messages"
    - "Test suite confirms conversation history is loaded (last 20 messages)"
    - "All existing tests still pass after main.py change"
  artifacts:
    - path: "backend/app/main.py"
      provides: "chat router registered at /api prefix"
      contains: "chat_router"
    - path: "backend/tests/api/test_chat.py"
      provides: "pytest test module covering all major chat code paths"
      exports: []
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/chat.py"
      via: "from app.api.chat import router as chat_router"
      pattern: "chat_router"
    - from: "backend/tests/api/test_chat.py"
      to: "backend/app/api/chat.py"
      via: "TestClient with patched get_db_path and state singletons"
      pattern: "patch.*app\\.api\\.chat"
---

<objective>
Register the chat router in main.py and write tests for backend/tests/api/test_chat.py covering mock mode, fallback, persistence, and history loading.

Purpose: Without registration the endpoint is unreachable. Without tests there is no automated verification that the full pipeline (mock mode, fallback, persistence, history) works correctly.

Output:
- backend/app/main.py — one new import and one new include_router line
- backend/tests/api/test_chat.py — pytest test module (~6-8 tests)
</objective>

<execution_context>
@C:\Users\Michele\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Michele\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-llm-chat-integration/03-CONTEXT.md
@.planning/phases/03-llm-chat-integration/03-01-SUMMARY.md

<interfaces>
<!-- Extracted from existing backend files — executor reads these to replicate patterns. -->

From backend/app/main.py (current state, lines 16-17 and 63-64):
  from app.api.portfolio import record_portfolio_snapshot, router as portfolio_router  # noqa: E402
  from app.api.watchlist import router as watchlist_router  # noqa: E402
  app.include_router(portfolio_router, prefix="/api")
  app.include_router(watchlist_router, prefix="/api")

  New lines to add (D-08):
  from app.api.chat import router as chat_router  # noqa: E402
  app.include_router(chat_router, prefix="/api")

From backend/tests/api/test_portfolio.py (test pattern to replicate):
  - db_path fixture: tmp_path / "test.db", init_db(path)
  - mock_cache fixture: MagicMock(), cache.get_price.return_value = 150.0,
    price_update = MagicMock(); price_update.price = 150.0; cache.get_all.return_value = {"AAPL": price_update}
  - _patched_client context manager: patches get_db_path, app.state.price_cache, app.state.market_source
  - Uses TestClient(app) from fastapi.testclient

From backend/app/api/chat.py (produced by Plan 01):
  - router = APIRouter(tags=["chat"])
  - POST /chat (becomes /api/chat after prefix)
  - LLMResponse model: message: str, trades: list[TradeAction], watchlist_changes: list[WatchlistChange]
  - Mock response shape: {"message": "Mock: I'll buy 1 AAPL...", "trades": [...], "watchlist_changes": [...]}
  - Fallback response shape: {"message": "I'm temporarily unavailable...", "trades": [], "watchlist_changes": []}
  - Persistence: chat_messages table gets user row (before LLM) and assistant row (after)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register chat router in main.py</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py — current imports and include_router calls (lines 16-17 and 62-64) to replicate the exact style
    - backend/app/api/chat.py — confirm the router export name is "router"
  </read_first>
  <action>
    Edit backend/app/main.py to add two lines following the established pattern (D-08):

    In the imports section after the existing watchlist_router import, add:
      from app.api.chat import router as chat_router  # noqa: E402

    After the existing app.include_router(watchlist_router, prefix="/api") line, add:
      app.include_router(chat_router, prefix="/api")

    No other changes. Match the existing # noqa: E402 comment style on the import line.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev python -c "from app.main import app; routes = [r.path for r in app.routes]; assert '/api/chat' in routes, f'chat not found: {routes}'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/main.py contains "from app.api.chat import router as chat_router"
    - backend/app/main.py contains "app.include_router(chat_router, prefix=\"/api\")"
    - The Python check above exits 0 with output "ok"
    - uv run --extra dev pytest tests/api/test_portfolio.py exits 0 (existing tests unaffected)
  </acceptance_criteria>
  <done>GET /api/chat route is registered and reachable; existing routes unchanged</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write tests for chat.py</name>
  <files>backend/tests/api/test_chat.py</files>
  <read_first>
    - backend/tests/api/test_portfolio.py — full file: db_path fixture, mock_cache fixture, _patched_client pattern with patch targets, TestClient usage
    - backend/app/api/chat.py — full file produced by Plan 01: route handler logic, mock detection, fallback, persistence, LLMResponse model
    - backend/app/db/init_db.py — init_db signature (accepts optional path argument)
  </read_first>
  <action>
    Create backend/tests/api/test_chat.py. Replicate the fixture and patching pattern from test_portfolio.py exactly. Use a _patched_chat_client context manager that patches: app.api.chat.get_db_path (returns db_path), app.state.price_cache (mock_cache), app.state.market_source (AsyncMock).

    In the mock_cache fixture, also set cache.get_all to return {"AAPL": price_update, "PYPL": price_update} so watchlist context building works.

    Write these test functions:

    test_chat_mock_mode: Set env var LLM_MOCK=true (use monkeypatch.setenv). POST /api/chat with {"message": "hello"}. Assert response status 200. Assert response JSON has keys "message", "trades", "watchlist_changes". Assert response JSON "message" starts with "Mock:".

    test_chat_mock_executes_trade: Set LLM_MOCK=true. Seed AAPL in watchlist and enough cash in DB. POST /api/chat. Assert a trade was inserted in trades table (SELECT COUNT(*) FROM trades > 0).

    test_chat_mock_adds_watchlist: Set LLM_MOCK=true. POST /api/chat. Assert PYPL appears in watchlist table (SELECT ticker FROM watchlist WHERE ticker='PYPL').

    test_chat_llm_fallback: Do NOT set LLM_MOCK. Patch litellm.completion (target: app.api.chat.completion) to raise Exception("LLM down"). POST /api/chat with {"message": "hello"}. Assert status 200. Assert response JSON "message" == "I'm temporarily unavailable. Please try again." and "trades" == [] and "watchlist_changes" == [].

    test_chat_persists_user_message: Set LLM_MOCK=true. POST /api/chat with {"message": "test message"}. Query chat_messages WHERE role='user'. Assert at least one row with content="test message".

    test_chat_persists_assistant_message: Set LLM_MOCK=true. POST /api/chat. Query chat_messages WHERE role='assistant'. Assert at least one row exists with non-empty content.

    test_chat_history_included: Set LLM_MOCK=true. Insert 3 rows into chat_messages (2 user, 1 assistant) with created_at values in the past. Then POST /api/chat. Assert the response is still 200 (history loading did not break the call). Assert chat_messages table has at least 5 rows after (3 pre-existing + 1 user + 1 assistant).

    test_chat_fallback_persists_messages: Patch app.api.chat.completion to raise Exception. POST /api/chat with {"message": "failing message"}. Assert status 200. Query chat_messages: expect both a user row with content="failing message" and an assistant row with the fallback message.

    All DB assertions use sqlite3.connect(db_path) directly — do not go through the HTTP API for verification. Use tmp_path db_path fixture so each test is isolated. Use monkeypatch for env vars so they are reset between tests.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/api/test_chat.py -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/api/test_chat.py exists
    - uv run --extra dev pytest tests/api/test_chat.py -v exits 0
    - Output shows at least 8 test functions collected and passing
    - uv run --extra dev pytest tests/ -v exits 0 (all existing tests still pass)
    - test_chat_llm_fallback passes: patches litellm.completion to raise, confirms HTTP 200 with fallback message
    - test_chat_mock_mode passes: LLM_MOCK=true returns 200 with Mock: message
    - test_chat_persists_user_message passes: user message in chat_messages before LLM call completes
  </acceptance_criteria>
  <done>Test suite passes with all 8+ tests green; full test suite exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> app | Tests use TestClient and patched DB — no real network or OpenRouter calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | router registration order in main.py | accept | Single-user app; route order does not create security boundary; FastAPI matches most specific path first |
| T-03-08 | Information Disclosure | test DB in tmp_path | accept | Tests run in isolated tmp directories; no real data; no credentials in test fixtures |
</threat_model>

<verification>
After both tasks complete:
- uv run --extra dev pytest tests/ -v exits 0 (all tests pass including new chat tests)
- backend/app/main.py contains chat_router registration
- POST /api/chat reachable: uv run --extra dev python -c "from app.main import app; routes = [r.path for r in app.routes]; assert '/api/chat' in routes"
</verification>

<success_criteria>
- POST /api/chat is registered and reachable at /api/chat prefix
- 8+ chat tests pass covering: mock mode, trade execution, watchlist mutation, fallback, persistence, history loading
- Full test suite (uv run --extra dev pytest tests/) exits 0
</success_criteria>

<output>
After completion, create .planning/phases/03-llm-chat-integration/03-02-SUMMARY.md
</output>
