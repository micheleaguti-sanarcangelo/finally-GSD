---
phase: 04-frontend
plan: 04
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - frontend/src/components/MainChart.tsx
  - frontend/src/components/PortfolioHeatmap.tsx
  - frontend/src/components/PnLChart.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "MainChart renders an AreaChart of price-over-time for the selected ticker using SSE-accumulated history"
    - "Clicking a ticker in WatchlistPanel and seeing the MainChart switch to that ticker's data"
    - "PortfolioHeatmap renders a Recharts Treemap with one rectangle per position, sized by portfolio weight"
    - "Treemap rectangles are green when unrealized_pnl > 0, red when < 0"
    - "PnLChart renders a Recharts AreaChart of total portfolio value over time from /api/portfolio/history"
    - "PnLChart re-fetches data when the parent calls its refresh prop after a trade"
  artifacts:
    - path: "frontend/src/components/MainChart.tsx"
      provides: "AreaChart of price history for selected ticker"
      exports: ["MainChart"]
    - path: "frontend/src/components/PortfolioHeatmap.tsx"
      provides: "Treemap of positions by weight colored by P&L"
      exports: ["PortfolioHeatmap"]
    - path: "frontend/src/components/PnLChart.tsx"
      provides: "Area chart of portfolio value snapshots over time"
      exports: ["PnLChart"]
  key_links:
    - from: "frontend/src/components/MainChart.tsx"
      to: "frontend/src/lib/store.ts"
      via: "usePriceStore(state => state.prices) for live price accumulation"
      pattern: "usePriceStore"
    - from: "frontend/src/components/PnLChart.tsx"
      to: "/api/portfolio/history"
      via: "fetch('/api/portfolio/history') on mount and on historyVersion change"
      pattern: "api/portfolio/history"
    - from: "frontend/src/components/PortfolioHeatmap.tsx"
      to: "frontend/src/lib/store.ts"
      via: "usePriceStore for live current price (position weight calculation)"
      pattern: "usePriceStore"
---

<objective>
Build the three Recharts-powered visualizations: MainChart (price history for selected ticker), PortfolioHeatmap (treemap of positions), and PnLChart (portfolio value over time). These are the charts that make the app look like a real trading terminal.

Purpose: Together these three charts give the user the "Bloomberg terminal" experience — visual proof that prices move, positions have P&L, and portfolio value tracks over time.
Output: MainChart.tsx, PortfolioHeatmap.tsx, PnLChart.tsx — TypeScript-valid Recharts components ready for layout assembly in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-01-SUMMARY.md
@planning/PLAN.md
</context>

<interfaces>
<!-- From frontend/src/lib/store.ts (Plan 01) -->
```typescript
interface PriceUpdate {
  ticker: string;
  price: number;
  previous_price: number;
  timestamp: number;
  change: number;
  change_percent: number;
  direction: 'up' | 'down' | 'flat';
}
export declare const usePriceStore: (selector: (s: PriceStore) => any) => any;
```

<!-- From backend GET /api/portfolio/history -->
```typescript
interface PortfolioHistoryResponse {
  history: Array<{
    total_value: number;
    recorded_at: string;  // ISO timestamp string
  }>;
}
```

<!-- Position shape from GET /api/portfolio (passed as props to PortfolioHeatmap) -->
```typescript
interface Position {
  ticker: string;
  quantity: number;
  avg_cost: number;
  current_price: number;
  unrealized_pnl: number;
  pnl_percent: number;
}
```

<!-- Recharts Treemap data shape -->
```typescript
// Each child in Treemap data needs: name, size, and any extra fields for styling
interface TreemapNode {
  name: string;     // ticker
  size: number;     // portfolio weight in dollars
  pnl: number;      // unrealized_pnl for color
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MainChart — live price history AreaChart for selected ticker</name>
  <files>frontend/src/components/MainChart.tsx</files>
  <read_first>
    - .planning/phases/04-frontend/04-CONTEXT.md (D-01, D-07, D-09)
    - planning/PLAN.md (Section 10 — main chart area spec: "larger chart for selected ticker, price over time")
    - The interfaces block above (PriceUpdate shape from Zustand)
  </read_first>
  <action>
    Create frontend/src/components/MainChart.tsx as a client component.

    Props interface:
    ```
    interface MainChartProps {
      ticker: string | null;
    }
    ```

    State:
    - `priceHistory: Record<string, Array<{ time: number; price: number }>>` — accumulated from SSE since page load, keyed by ticker. Keep last 200 points per ticker.

    SSE integration:
    - Subscribe to `usePriceStore(state => state.prices)`
    - In useEffect on `prices` change: for each ticker in prices, if `prices[ticker]` has changed (check timestamp), append `{ time: prices[ticker].timestamp * 1000, price: prices[ticker].price }` to `priceHistory[ticker]`
    - Keep only last 200 points per ticker (`.slice(-200)`)

    Chart rendering (per D-01 — Recharts AreaChart):
    - If `ticker` is null or priceHistory[ticker] has < 2 points, show a placeholder: centered text in the chart area saying "Select a ticker from the watchlist"
    - Otherwise render a Recharts `<ResponsiveContainer width="100%" height="100%">` wrapping `<AreaChart data={priceHistory[ticker]}>`
    - `<XAxis dataKey="time">` — hide tick labels with `tick={false}` (too dense with 200 points)
    - `<YAxis domain={['auto', 'auto']}` — let Recharts auto-scale; `tickFormatter={(v) => \`$\${v.toFixed(0)}\``}; `tick={{ fill: '#94a3b8', fontSize: 11 }}`
    - `<Tooltip contentStyle={{ backgroundColor: '#1a1a2e', border: '1px solid #2a2a3a', color: '#e2e8f0' }} formatter={(value: number) => [\`$\${value.toFixed(2)}\`, 'Price']}` labelFormatter with timestamp
    - `<Area type="monotone" dataKey="price" stroke="#209dd7" fill="url(#chartGradient)" strokeWidth={2} dot={false} isAnimationActive={false} />`
    - Add a `<defs>` with a `<linearGradient id="chartGradient">` from `rgba(32,157,215,0.3)` at top to `rgba(32,157,215,0)` at bottom

    Display ticker name and current price above the chart: `{ticker} — ${ prices[ticker]?.price?.toFixed(2) ?? '---' }` in accent-yellow/accent-blue text.

    Set `isAnimationActive={false}` on the Area to prevent jarring re-animations on every price update.

    Export: `export function MainChart({ ticker }: MainChartProps)`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "MainChart\|error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 for MainChart.tsx
    - File imports `AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer` from `recharts`
    - File imports `usePriceStore` from `@/lib/store`
    - File contains `isAnimationActive={false}` on the Area element
    - File contains `linearGradient` definition with `209dd7` color
    - File contains `.slice(-200)` for history cap
    - File has `'use client'` directive and exports `MainChart` as named export
  </acceptance_criteria>
  <done>MainChart accumulates SSE price history per ticker and renders a live-updating Recharts AreaChart for the selected ticker</done>
</task>

<task type="auto">
  <name>Task 2: PortfolioHeatmap (Treemap) and PnLChart (AreaChart)</name>
  <files>
    frontend/src/components/PortfolioHeatmap.tsx
    frontend/src/components/PnLChart.tsx
  </files>
  <read_first>
    - .planning/phases/04-frontend/04-CONTEXT.md (D-01, D-08, D-11, D-12)
    - planning/PLAN.md (Section 10 — portfolio heatmap: treemap sized by weight, colored by P&L; P&L chart from /api/portfolio/history)
    - The interfaces block above (Position shape, PortfolioHistoryResponse shape, TreemapNode shape)
  </read_first>
  <action>
    Create frontend/src/components/PortfolioHeatmap.tsx as a client component.

    Props interface:
    ```
    interface PortfolioHeatmapProps {
      positions: Array<{
        ticker: string;
        quantity: number;
        avg_cost: number;
        unrealized_pnl: number;
      }>;
    }
    ```

    Live price integration: subscribe to `usePriceStore(state => state.prices)`. Compute each position's current market value as `(prices[ticker]?.price ?? avg_cost) * quantity` for the treemap `size` field.

    Build treemap data: for each position, create `{ name: ticker, size: marketValue, pnl: (prices[ticker]?.price ?? avg_cost) - avg_cost }`.

    If positions is empty: render centered text "No positions yet".

    Render a Recharts `<ResponsiveContainer width="100%" height="100%">` wrapping `<Treemap data={[{ name: 'positions', children: treemapData }]} dataKey="size" aspectRatio={4/3} isAnimationActive={false}>`.

    Custom content renderer for each cell — use Recharts Treemap `content` prop with a function receiving `{ x, y, width, height, name, pnl }`:
    - Fill color: `pnl > 0 ? '#16a34a' : pnl < 0 ? '#dc2626' : '#374151'` (green/red/gray)
    - Render: a `<rect>` with the fill color and a subtle stroke `#0d1117`
    - If width > 60 and height > 30: render `<text>` centered in the rect with ticker name (white, bold, small font)
    - If width > 60 and height > 50: render a second `<text>` with the pnl formatted as `+$X.XX` or `-$X.XX` (smaller font, lighter color)

    Export: `export function PortfolioHeatmap({ positions }: PortfolioHeatmapProps)`

    ---

    Create frontend/src/components/PnLChart.tsx as a client component.

    Props interface:
    ```
    interface PnLChartProps {
      historyVersion: number;  // increment this to trigger a re-fetch (after trades)
    }
    ```

    State:
    - `history: Array<{ time: number; value: number }>` — mapped from API response

    On mount and when historyVersion changes: `fetch('/api/portfolio/history')`, parse PortfolioHistoryResponse, map to `{ time: new Date(h.recorded_at).getTime(), value: h.total_value }`. If history is empty after fetch, keep `history` as `[]`.

    Chart rendering:
    - If history has < 2 points: show "Portfolio history will appear after first trade" centered text
    - Otherwise: Recharts `<ResponsiveContainer width="100%" height="100%">` wrapping `<AreaChart data={history}>`
    - `<XAxis dataKey="time" tick={false} />`
    - `<YAxis domain={['auto', 'auto']} tickFormatter={(v) => \`$\${v.toLocaleString()}\`} tick={{ fill: '#94a3b8', fontSize: 11 }} />`
    - `<Tooltip contentStyle={{ backgroundColor: '#1a1a2e', border: '1px solid #2a2a3a', color: '#e2e8f0' }} formatter={(v: number) => [\`$\${v.toFixed(2)}\`, 'Portfolio Value']} />`
    - `<Area type="monotone" dataKey="value" stroke="#ecad0a" fill="url(#pnlGradient)" strokeWidth={2} dot={false} isAnimationActive={false} />`
    - `<defs>` with `<linearGradient id="pnlGradient">` from `rgba(236,173,10,0.3)` to `rgba(236,173,10,0)`

    Include a small label "Portfolio Value (USD)" above the chart.

    Export: `export function PnLChart({ historyVersion }: PnLChartProps)`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "Heatmap\|PnL\|error" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 for both PortfolioHeatmap.tsx and PnLChart.tsx
    - PortfolioHeatmap.tsx imports `Treemap, ResponsiveContainer` from `recharts`
    - PortfolioHeatmap.tsx imports `usePriceStore` for live price-based market value
    - PortfolioHeatmap.tsx contains color logic with `16a34a` (green) and `dc2626` (red)
    - PortfolioHeatmap.tsx contains `isAnimationActive={false}`
    - PnLChart.tsx imports `AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer` from `recharts`
    - PnLChart.tsx contains `fetch('/api/portfolio/history'`
    - PnLChart.tsx re-fetches when `historyVersion` changes (useEffect dependency array contains `historyVersion`)
    - PnLChart.tsx contains `linearGradient` with `ecad0a`
    - Both files have `'use client'` directive and named exports
  </acceptance_criteria>
  <done>PortfolioHeatmap renders live-priced Recharts Treemap colored by P&L; PnLChart renders portfolio value history as an AreaChart and re-fetches when historyVersion increments</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/portfolio/history | Read-only fetch; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | Recharts Treemap custom content | accept | Data is from same-origin API; no user-controlled HTML rendered in SVG cells |
| T-04-10 | Denial of Service | PnLChart refetch on historyVersion | accept | Triggered only by user trade actions, not continuous polling; negligible risk |
</threat_model>

<verification>
After both tasks complete:
- `cd frontend && npx tsc --noEmit` exits 0
- `grep "recharts" src/components/MainChart.tsx src/components/PortfolioHeatmap.tsx src/components/PnLChart.tsx` all match
- `grep "usePriceStore" src/components/MainChart.tsx src/components/PortfolioHeatmap.tsx` all match
- `grep "api/portfolio/history" src/components/PnLChart.tsx` matches
- `grep "16a34a.*dc2626\|dc2626.*16a34a" src/components/PortfolioHeatmap.tsx` or individual greps match
</verification>

<success_criteria>
- MainChart accumulates per-ticker price history from Zustand SSE store and renders live-updating AreaChart
- PortfolioHeatmap renders Recharts Treemap with positions sized by live market value and colored green/red by P&L
- PnLChart fetches portfolio history and re-fetches on historyVersion increment, renders AreaChart with accent-yellow gradient
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend/04-04-SUMMARY.md` using the summary template.
</output>
